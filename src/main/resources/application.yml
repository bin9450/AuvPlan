server:
  port: 8080

mybatis:
  mapper-locations: classpath:mapping/*Mapping.xml
  type-aliases-package: com.auv.entity

spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/dronevr?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
  mvc:
    static-path-pattern: /static/*